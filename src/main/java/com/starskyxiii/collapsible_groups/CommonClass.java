package com.starskyxiii.collapsible_groups;

/**
 * Shared initialization entry point used by all loader-specific entry points.
 */
public class CommonClass {

	public static void init() {
		// Reserved for common startup hooks that must run on every loader.
	}
}
